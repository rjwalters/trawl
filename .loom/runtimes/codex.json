{
  "runtime": "codex",
  "capabilities": {
    "mcp": "yes",
    "subagents": "no",
    "hooks": "partial",
    "skills": "partial",
    "worktreeIsolation": "partial"
  },
  "capabilityGate": {
    "issue": 4495,
    "epic": 4489,
    "codexSchemaPin": "0.146.0",
    "note": "hooks/worktreeIsolation stay 'partial' until the evidence gate below is satisfied. The pre_tool_use bridge (defaults/hooks/guard-codex-bridge.sh) and its managed provisioning (defaults/scripts/provision-codex-hooks.sh) are implemented and CI-tested, but promotion additionally requires live-CLI evidence that is not yet recorded. The #4478 operator sandbox-posture decision IS recorded (2026-07-31, read-only default + Builder-only workspace-write escalation) and the adapter already matches it, so #4478 is no longer what blocks promotion — the technical evidence below is. Do not flip these values without appending the evidence links to defaults/docs/guardrail-parity-codex.md.",
    "pending": [
      "a documented non-interactive Codex hook-trust path, or an accepted operator-attested one-time trust step; today verify falls back to the coarse hooks.state trusted_hash signal. This blocks the canary below: an untrusted hook does not run, so a headless canary would prove nothing",
      "real Codex CLI canary: one allowed managed-worktree mutation plus denials for a main-checkout mutation and a protected-branch destructive shell command, with before/after filesystem+git evidence",
      "confirm hooks.json matcher semantics on the live CLI (Loom emits the Claude-compatible \"*\" wildcard; the 0.146.0 binary does not document whether the matcher is a regex or a glob)",
      "write_stdin remains an un-inspectable mutation surface and is denied outright, so 'every supported mutating tool path is confined' holds only by exclusion",
      "a hook SIGKILLed after Codex's timeout grace period emits nothing, and on this wire silence means allow; the bridge's EXIT trap covers crash and SIGTERM but SIGKILL is untrappable"
    ]
  }
}
